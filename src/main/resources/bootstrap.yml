spring:
  application:
    name: techbyte-demo
  cloud:
    kubernetes:
      reload:
        enabled: true

---
spring:
  profiles: local,unit-test,functional-test
  cloud:
    kubernetes:
      enabled: false
